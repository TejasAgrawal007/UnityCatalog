## 1. Creating Dev and QA catlog

%sql
create catalog if not exists dev;


%sql
create catalog if not exists qa;

%sql
create database if not exists dev.demo_db;

%sql
drop database if exists qa.default;

